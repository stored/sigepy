test:
    image: python:3-alpine
    script:
        - pip install pytest pytest-cov httpretty coveralls
        - pip install .
        - pip install -r requirements.txt
        - python py.test --cov=sigep --cov-config .coveragerc -v --color=yes --showlocals tests/