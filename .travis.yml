language: python
before_install:
    - pip install pytest pytest-cov httpretty
install:
  - pip install .
  - pip install -r requirements.txt
script:
    - py.test -v --color=yes --exitfirst --showlocals